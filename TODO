Remove commented-out code from table.c:
  1. I commented out extern int sys_task() ... printer_task().
  2. I also commented out int (*task[ ... sys_task, 0, 0, 0, 0 };

The reason this code is commented out is because I needed table.c in the compilation process so struct proc would have memory (and not be preceeded by EXTERN).

I need struct proc in main.c so the program will compile.
